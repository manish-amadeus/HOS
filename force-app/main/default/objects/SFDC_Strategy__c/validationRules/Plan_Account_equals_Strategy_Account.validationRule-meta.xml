<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Plan_Account_equals_Strategy_Account</fullName>
    <active>true</active>
    <errorConditionFormula>SFDC_Acct_Plan__r.Account__c &lt;&gt; Account__c</errorConditionFormula>
    <errorDisplayField>Account__c</errorDisplayField>
    <errorMessage>The Account linked to an Account Strategy must be the same Account that is linked to the related Account Plan.</errorMessage>
</ValidationRule>
