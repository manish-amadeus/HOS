<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Corporate_Group__c</fullName>
    <description>Salesforce.com Change Request: NICC-016642</description>
    <externalId>false</externalId>
    <formula>IF(
RIGHT(
LEFT(Assigned_To__r.Email,LEN(Assigned_To__r.Email) - ( LEN(Assigned_To__r.Email)-FIND(&quot;.&quot;,Assigned_To__r.Email)+1 )),
LEN(LEFT(Assigned_To__r.Email,LEN(Assigned_To__r.Email) - ( LEN(Assigned_To__r.Email)-FIND(&quot;.&quot;,Assigned_To__r.Email)+1 )))- FIND(&quot;@&quot;,LEFT(Assigned_To__r.Email,LEN(Assigned_To__r.Email) - ( LEN(Assigned_To__r.Email)-FIND(&quot;.&quot;,Assigned_To__r.Email)+1 )))
) = &quot;cybage&quot;,&quot;cybage&quot;,&quot;Newmarket&quot;
)

/*
RIGHT(
LEFT(Assigned_To__r.Email,LEN(Assigned_To__r.Email) - ( LEN(Assigned_To__r.Email)-FIND(&quot;.&quot;,Assigned_To__r.Email)+1 )),
LEN(LEFT(Assigned_To__r.Email,LEN(Assigned_To__r.Email) - ( LEN(Assigned_To__r.Email)-FIND(&quot;.&quot;,Assigned_To__r.Email)+1 )))- FIND(&quot;@&quot;,LEFT(Assigned_To__r.Email,LEN(Assigned_To__r.Email) - ( LEN(Assigned_To__r.Email)-FIND(&quot;.&quot;,Assigned_To__r.Email)+1 )))
)
*/</formula>
    <inlineHelpText>The team that is responsible for the task</inlineHelpText>
    <label>Corporate Group</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
