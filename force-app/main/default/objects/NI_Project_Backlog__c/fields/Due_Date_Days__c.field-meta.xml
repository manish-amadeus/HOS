<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Due_Date_Days__c</fullName>
    <description>Used to compare against the estimated LOE so that we can escalate if necessary (if value is negative, then we don&apos;t have time to complete the project in time even if we started it immediately).</description>
    <externalId>false</externalId>
    <formula>IF( ISPICKVAL(Request_Status__c,&quot;Completed&quot;), NULL,
Requested_Completion_Date__c - TODAY() )</formula>
    <inlineHelpText>Amount of time between now and the requested due date (blank if project is completed).</inlineHelpText>
    <label>Days until Due Date</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
