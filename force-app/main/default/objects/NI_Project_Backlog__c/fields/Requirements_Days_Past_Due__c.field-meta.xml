<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Requirements_Days_Past_Due__c</fullName>
    <description>NICC-044048
The number of days the requirements are past due.  Projects with past due requirements are at risk of being de-prioritized</description>
    <externalId>false</externalId>
    <formula>IF(Requirements_Due_Date__c &lt; Requirements_Received_Date__c,Requirements_Received_Date__c - Requirements_Due_Date__c, 
 IF(ISBLANK(Requirements_Received_Date__c) &amp;&amp; TODAY() &gt; Requirements_Due_Date__c, TODAY()- Requirements_Due_Date__c,0
  
    )
   )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>The number of days the requirements are past due.  Projects with past due requirements are at risk of being de-prioritized</inlineHelpText>
    <label>Requirements Days Past Due</label>
    <precision>18</precision>
    <required>false</required>
    <scale>1</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
