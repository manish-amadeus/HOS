<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Queue__c</fullName>
    <description>Enter Queue Name (Label), which is used to give access to Kanban dashboard. It is not mandatory, we can skip it if we want to give permission by using Permission set or Group.</description>
    <encryptionScheme>None</encryptionScheme>
    <externalId>false</externalId>
    <inlineHelpText>Enter Queue Name (Label), which is used to give access to Kanban dashboard. It is not mandatory, we can skip it if we want to give permission by using Permission set or Group.</inlineHelpText>
    <label>Queue</label>
    <length>60</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
